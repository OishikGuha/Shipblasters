using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CustomizerList : MonoBehaviour
{

    public List<CustomizerSelection> hulls;
    public List<CustomizerSelection> turrets;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
